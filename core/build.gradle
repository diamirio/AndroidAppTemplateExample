apply from: rootProject.file("library-module.gradle")

dependencies {
    api deps.kotlin.values()

    /**
     * Base
     */
    api deps.reaktor.core
    implementation deps.androidutil.network
    implementation deps.androidutil.optional

    /**
     * DI
     */
    api deps.koin.values()

    /**
     * Rx
     */
    api deps.rxjava.core
    api deps.rxjava.kotlin
    api deps.rxjava.relay

    /**
     * Networking
     */
    implementation deps.networking.values()
    api deps.networking.retrofit

    /**
     * Persistence
     */
    implementation deps.room.core
    kapt deps.room.compiler
    implementation deps.room.rxjava
    debugImplementation deps.room.debug

    /**
     * Util
     */
    api deps.coreutil.values()

    /**
     * Testing
     */
    testImplementation deps.test.values()
}
